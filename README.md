# Feishu CLI MCP Server

A modular Model Context Protocol (MCP) server that exposes controlled Feishu/Lark document and Base operations through the official [lark-cli](https://github.com/larksuite/cli).

> Work in progress. The first implementation is being developed on a feature branch.
